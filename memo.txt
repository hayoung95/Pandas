### 클린 명령어 : cls
### cd C:\dev\workspace\python\Pandas
저는 제가 만든 전처리 프로그램을 소개해드리려고 하는데요
간단하게 전처리가 정상적으로 처리가 되면, 시퀀셜하게 관리되는 차수를 기준으로 새로운 폴더에
데이터파일이 저장되고, 테이블에 기록을 남기는것 까지 진행해봤습니다.


from datetime import datetime
       <모듈>            <클래스>

개념이 다르다.
자바에서의 모듈은 여러 클래스나 패키지를 통틀어 칭하는데
파이선은 def 함수들이 모여있는 .py 파일을 모듈이라고 부른다.

예외 처리 구문 / 트랜젝션

strftime

# datetime
날짜와 시간을 객체로 다룰 수 있게 해주는 내장 모듈
strftime : 날짜 객체를 > 문자열로 변환해주는 메서드


# OS.PATH
파일이나 디렉토리의 경로를 다룰 때 사용하고
basename : 파일 이름만 추출
splitext : 확장자 분리(튜플로 만든다) 첫번째 요소에 파 일명


# pandas
.read_csv : header는 몇번째 줄을 컬럼명으로 사용하는지에 대한 설정값
              따로 컬럼명을 주기위해 names= 속성을 사용하기 위해서
	header에 None값을 주었다

df.apply() : dataframe에서 행 또는 열 단위로 함수를 적용
              axis=1을 주면 row단위로 함수를 적용한다.
map(str, row)는 row안의 각 값을 문자열로 변환
''.join : 문자열 리스트를 구분자 없이 하나의 문자열로 합치는 함수

def 키워드로 정의된 함수들이 모여있는 py파일을 모듈이라고 부른다

series는 1차원 데이터 구조. 인덱스 + 값으로 이루어짐
dataframe은 2차원 데이터 구조

run_number = cursor.lastrowid 의 lastrowid는 속성이다.

connection 은 db에 연결된 상태 갖고있고
cursor는 sql문을 실행하고 결과를 가져온다.
lastrowid는 insert 될때에 최신화됌

__main__ 은 프로그램의 시작점을 의미한다.
// 자체적으로 실행하려고 사용하는 것.